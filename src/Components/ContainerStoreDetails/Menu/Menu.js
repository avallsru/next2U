import React from 'react';
// import PropTypes from 'prop-types';

const Menu = props => {
  return (
    <div>
      This is the menu
    </div>
  );
};

// Menu.propTypes = {
  
// };

export default Menu;