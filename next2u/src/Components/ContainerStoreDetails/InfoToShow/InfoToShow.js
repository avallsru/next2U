import React from 'react';
import withSelector from '../../../HOC/withSelector';

const InfoToShow = (kind) => {
  return (
    <div>
      hello
    </div>
  );
};

export default withSelector(InfoToShow);